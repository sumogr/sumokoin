name: Continuous_Integration_Testing

on: [push, pull_request]

jobs:
  Windows32-cross-on-focal-fossa:
    runs-on: ubuntu-20.04
    container:
      image: node:10.24.0-alpine3.11:i386
    steps:
    - name: Log the node version
      run: |
        node -v
        cat /etc/os-release
