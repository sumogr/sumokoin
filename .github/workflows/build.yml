name: Continuous_Integration_Testing

on: [push, pull_request]

jobs:
  build-windows:
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@v1
    - name: install msys2
      run: |
        C:\msys64\usr\bin\bash -lc ' '
        # Update MSYS2
        C:\msys64\usr\bin\bash -lc 'pacman --noconfirm -Syuu'  # Core update (in case any core packages are outdated)
        C:\msys64\usr\bin\bash -lc 'pacman --noconfirm -Syuu'  # Normal update    
    - name: install sumokoin dependencies
      run: |
           $env:CHERE_INVOKING = 'yes'  # Preserve the current working directory
           $env:MSYSTEM = 'MINGW64'  # Start a 64 bit Mingw environment
           C:\msys64\usr\bin\bash -lc 'pacman -S --noconfirm mingw-w64-x86_64-openssl mingw-w64-x86_64-zeromq mingw-w64-x86_64-libsodium git'     
    - name: build sumokoin
      run: |
         cd D:\a\sumokoin\sumokoin
         make release-static-win64 -j2
