name: Continuous_Integration_Testing

on: [push, pull_request]

jobs:

  cross-build-linux-32bit-binaries-on-xenial:
    runs-on: ubuntu-latest
    steps:
    - name : install lxd container
      run: |
        sudo apt-get update
        sudo apt-get install snap
        sudo snap install lxd --channel=4.0/stable
        sudo lxd init
        sudo lxc launch ubuntu:20.04 ubuntucross
    - uses: actions/checkout@v1
    - name: push sumokoin folder to container
      run:  sudo lxc file push -r /home/runner/work/sumokoin/sumokoin/ ubuntucross/root/
    - name: login to container console
      run: |
        sudo lxc exec ubuntucross -- /bin/bash
        sudo lxc exec containername -- apt-get update
        sudo lxc exec containername -- apt-get install g++-multilib bc
     






