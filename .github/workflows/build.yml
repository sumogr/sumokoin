name: Continuous_Integration_Testing

on: [push, pull_request]

jobs:

  cross-build-linux-32bit-binaries-on-xenial:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v1
    - name: clean ubuntu
      run: |
        sudo apt-get autoremove --purge
        sudo apt remove *c++* --allow-remove-essential
        sudo apt remove *cc* --allow-remove-essential
        sudo apt remove *python* --allow-remove-essential
        sudo apt remove *google* --allow-remove-essential
        sudo apt remove *ld* --allow-remove-essential
        sudo apt remove *php* --allow-remove-essential
        sudo apt remove *google* --allow-remove-essential
        sudo apt remove *azure* --allow-remove-essential
        sudo apt remove *ruby* --allow-remove-essential
        sudo apt remove *mysql* --allow-remove-essential
        
